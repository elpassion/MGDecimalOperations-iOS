platform :ios, '8.0'

target 'MGDecimalOperations' do
  target 'MGDecimalOperationsTests' do
    inherit! :search_paths
    pod 'Expecta', '~> 1.0'
    pod 'OCMockito', '~> 3.0'
    pod 'Specta', '~> 1.0'
  end
end
